import InputAppName from "@site/src/components/AppName/InputAppName";
import AppName from "@site/src/components/AppName/AppName";

import NpmInstall from "@site/src/components/CodeBlock/NpmInstall";
import CodeStructure from "@site/src/components/CodeBlock/CodeStructure";

import constants from "../../constants.json";

# Installation

How to install react native boilerplate and start your project

:::info
Default project name is MyApp , you can change it
:::

First you need to enter your project / application name.

<InputAppName />

<NpmInstall />

## Project structure {#project-structure}

Assuming you chose the classic template and named your application <AppName background />, you will see the following files generated under a new directory <AppName background />:

<CodeStructure />

### Project structure rundown {#project-structure-rundown}

- `/blog/` - Contains the blog Markdown files. You can delete the directory if you've disabled the blog plugin, or you can change its name after setting the `path` option. More details can be found in t
- `/docs/` - Contains the Markdown files for the docs. Customize the order of the docs sidebar in `sidebars.js`. You can delete the directory if you've disabled the docs plugin, or you can change its name after setting the `path` option. More details can be found in the
- `/src/` - Non-documentation files like pages or custom React components. You don't have to strictly put your non-documentation files here, but putting them under a centralized directory makes it easier to specify in case you need to do some sort of linting/processing
- `/src/pages` - Any JSX/TSX/MDX file within this directory will be converted into a website page. More details can be found in the
- `/static/` - Static directory. Any contents inside here will be copied into the root of the final `build` directory
- `/docusaurus.config.js` - A config file containing the site configuration. This is the equivalent of `siteConfig.js` in Docusaurus v1
- `/package.json` - A Docusaurus website is a React app. You can install and use any npm packages you like in them
- `/sidebars.js` - Used by the documentation to specify the order of documents in the sidebar

<br />
<br />

Congratulations! You have just created your <AppName background/> application!

## Problems? ⚡ {#problems}

Ask for help on our [GitHub repository](https://github.com/adsalihac/react-native-boilerplate/discussions).
